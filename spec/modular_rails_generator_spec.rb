RSpec.describe ModularRailsGenerator do
  it "has a version number" do
    expect(ModularRailsGenerator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
